// ? View engines are useful for rendering web pages.
// ? There are many view engines available in the market like Mustache, Handlebars, EJS, etc
// ? but the most popular among them is EJS which simply stands for Embedded JavaScript.
// ? It is a simple templating language / engine that lets its user generate HTML with plain javascript.


const express = require('express');

const app = express();


